<table class="table table-striped">
<!-- this is a partial of a list of users for the user index page -->
  <thead>
    <tr>
      <th>Item Name</th>
      <th>Quantity</th>
      <th>Remove Item</th>
    </tr>
  </thead>
  <tbody>
    <% @items_in_cart.each do |oi| %>
      <tr>
        <td><%= link_to oi.item.name, home_path %></td>
        <td><%= oi.quantity %></td>
        <td> <%= link_to 'Remove Item', yourcart_path, :class => 'button radius' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br />